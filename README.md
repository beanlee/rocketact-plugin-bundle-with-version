# rocketact-plugin-bundle-with-version

To Bundle with the version which is in `package.json` config.

```bash
npm install --save-dev rocketact-plugin-bundle-with-version
or
yarn add -D rocketact-plugin-bundle-with-version
```
